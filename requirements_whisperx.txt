--extra-index-url https://download.pytorch.org/whl/cu121

# PyTorch — CUDA 12.1 (compatible con RTX 3060)
torch==2.5.1+cu121
torchaudio==2.5.1+cu121
torchvision==0.20.1+cu121

# WhisperX + faster-whisper backend
whisperx>=3.4.0
faster-whisper

# Pyannote — diarización
pyannote.audio>=3.3.2

# huggingface_hub < 1.0 — OBLIGATORIO porque whisperx y pyannote usan
# use_auth_token que fue ELIMINADO en huggingface_hub 1.0+
huggingface_hub>=0.24.0,<1.0

# transformers < 5.0 — para mantener compatibilidad con huggingface_hub < 1.0
transformers>=4.48.0,<5.0

# DB / Queue / Config
redis
rq
python-dotenv
psycopg2-binary
SQLAlchemy
certifi

# Monitoreo de recursos del sistema (CPU, RAM)
psutil>=5.9.0

# Métricas de evaluación WER
jiwer>=3.0
